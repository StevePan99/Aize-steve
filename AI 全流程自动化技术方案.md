# AI 全流程自动化技术方案
> **适用公司**：AZSL / 艾泽 / AITRI  
> **文档定位**：作为“从想法到上线”全流程 AI 自动化的独立技术方案参考  
> **适用前提**：公司内部协作以 Lark 为主，对外沟通以 Telegram 为主，当前媒体平台数据接入仍存在现实约束  
> **核心原则**：AI 主导流程，关键节点人工审批，执行过程可审计、可回滚

---

## 一、目标定义
本方案讨论的不是单点 AI 工具，而是一套覆盖“想法提出、需求澄清、方案设计、任务拆解、代码生成、测试、部署、复盘”的全流程自动化系统。
目标包括：
- 缩短从想法到上线的整体周期
- 降低跨团队沟通和项目推进成本
- 把需求、讨论、决策、代码、上线记录统一沉淀
- 让 AI 成为产研与运营流程的主协调者
- 在关键风险节点保留人工审批与兜底

---

## 二、总体判断
对 AZSL 当前阶段，最合理的目标不是一开始做“完全无人值守”的全自动系统，而是先做：
- AI 主导流程
- 人工审批关键节点
- 自动执行低风险动作
- 所有过程自动沉淀
更准确地说，这是一套 **Human-on-the-loop** 的 AI 自动化体系：
- AI 负责理解、拆解、编排、生成、推进
- 人负责审批、兜底、策略判断、风险确认
这样更符合你们当前：
- 10 到 15 人产研团队规模
- 40 到 50 名投手的协作需求
- 业务数据与媒体数据尚未完全自动打通的现实约束

---

## 三、适用范围
这套方案尤其适合以下场景：
- 新功能需求从沟通到上线
- 内部项目从讨论到排期到执行
- Lark / Telegram 沟通内容自动沉淀
- 技术方案和开发任务自动拆解
- 代码、测试、部署流程标准化
- 上线复盘与知识回写
不建议当前阶段直接完全自动化的场景：
- 生产环境无审批直接发布
- 高风险投放动作自动执行
- 严重依赖实时媒体数据闭环的强自动优化
- 无规则边界的全量代码修改

---

## 四、总体架构
### 4.1 六层架构
建议采用六层架构：
1. **入口层**
2. **Agent 编排层**
3. **知识与上下文层**
4. **工具执行层**
5. **审批与风控层**
6. **观测与复盘层**

### 4.2 架构概览
```text
【入口层】
Lark / Telegram / Web Console / API
        │
        ▼
【Agent 编排层】
需求 Agent / PRD Agent / 方案 Agent / PM Agent / 研发 Agent / 测试 Agent / 发布 Agent / 复盘 Agent
        │
        ▼
【知识与上下文层】
公司知识库 / 项目文档 / 历史需求 / 会议纪要 / 代码仓库知识 / 投放 SOP / 客户资料
        │
        ▼
【工具执行层】
Git / 项目管理系统 / CI-CD / 数据库 / 部署系统 / 报表系统 / Lark / Telegram
        │
        ▼
【审批与风控层】
权限 / 审批 / 白名单动作 / 审计日志 / 回滚机制
        │
        ▼
【观测与复盘层】
日志 / Token 成本 / 执行状态 / 失败重试 / 上线效果 / 知识沉淀
```

---

## 五、各层设计
### 5.1 入口层
入口层负责接收想法、需求、问题、客户反馈和内部讨论。
推荐入口：
- Lark Bot
- Telegram Bot
- Web 控制台
- API 接口
适合接收的输入包括：
- “帮我把这个想法整理成 PRD”
- “帮我把这个客户需求拆成研发任务”
- “帮我总结这周项目进展”
- “帮我根据技术方案生成开发任务”

### 5.2 Agent 编排层
Agent 编排层负责多角色协同和任务流转，不建议只用一个“大而全”的总管 Agent。
推荐 Agent 角色：
- 需求分析 Agent
- PRD Agent
- 技术方案 Agent
- 项目经理 Agent
- 研发 Agent
- 测试 Agent
- 发布 Agent
- 复盘 Agent
推荐职责边界：
- 需求 Agent：把想法转成结构化需求
- PRD Agent：生成 PRD 和验收标准
- 方案 Agent：生成技术方案、接口和数据设计
- PM Agent：拆任务、排优先级、跟踪阻塞
- 研发 Agent：生成代码草稿、脚本和 SQL
- 测试 Agent：生成测试样例和回归清单
- 发布 Agent：组织发布前检查和回滚方案
- 复盘 Agent：汇总上线效果和问题

### 5.3 知识与上下文层
这是全流程自动化的底座。
建议沉淀：
- 公司背景与战略
- 客户资料与项目历史
- 投放方法论与 SOP
- 技术文档与接口规范
- 会议纪要与沟通记录
- 项目进度、问题、待办、决议
- 历史代码与模块能力
当前阶段需要特别考虑：
- Lark 内部沟通摘要
- Telegram 外部沟通摘要
- 项目进度自动沉淀
- 问题讨论与责任人归档

### 5.4 工具执行层
AI 不应直接无边界执行动作，而应通过标准工具执行。
建议接入的工具包括：
- Git 仓库
- 项目管理系统
- CI / CD
- Lark
- Telegram
- 数据库
- 报表系统
- 部署系统
- 日志与监控系统
推荐方式：
- 所有外部能力都封装成标准 Tool
- 所有执行动作都经过权限和规则校验
- 高风险动作默认不能由 Agent 直接执行

### 5.5 审批与风控层
这是整套方案能否真正上线的关键。
必须具备：
- 权限系统
- 审批流
- 白名单动作
- 审计日志
- 回滚机制
- 手动接管能力
建议默认规则：
- 生成 PR 可以自动化
- 合并主分支需要审批
- 生成部署计划可以自动化
- 生产发布必须人工确认
- 低风险标准动作可以白名单自动执行

### 5.6 观测与复盘层
每一次自动化过程都应被记录和复盘。
建议记录：
- Agent 执行轨迹
- 调用工具记录
- 使用的上下文
- Token 成本
- 失败原因
- 人工介入节点
- 最终产出结果
- 上线后效果

---

## 六、具体实现路径
### 6.1 系统拆分建议
如果要真正落地，不建议把整套系统做成单体大应用，更适合拆成以下 6 个核心模块：
| 模块 | 作用 | 推荐技术 |
|-----|------|---------|
| **AI Gateway** | 统一模型调用、路由、缓存、成本统计 | FastAPI + Redis |
| **Workflow Orchestrator** | 编排多 Agent、任务状态、重试与超时 | LangGraph / Temporal |
| **Knowledge Hub** | 管理知识库、向量检索、上下文拼装 | PostgreSQL + pgvector / Qdrant |
| **Tool Hub** | 封装 Git、Lark、Telegram、CI/CD、部署等工具 | FastAPI Tool Service |
| **Approval & Audit** | 审批流、权限、日志、回滚记录 | PostgreSQL + 审计日志 |
| **Ops Console** | 统一查看流程、任务、成本、审批和复盘 | Web Console |

### 6.2 推荐实现顺序
推荐按以下顺序实现，而不是同时铺开：
1. 先搭建 AI Gateway
2. 再搭建 Workflow Orchestrator
3. 再搭建 Knowledge Hub
4. 再接入 Tool Hub
5. 再补齐 Approval & Audit
6. 最后做 Ops Console
这样做的原因是：
- 可以先把模型调用和成本控制统一起来
- 可以先把流程骨架跑通
- 可以先让 AI 具备“知道什么”和“能做什么”
- 最后再做治理和运营视图

### 6.3 最小可运行链路
第一阶段建议只先跑通 2 条最小链路，避免把 MVP 做成“大而全”：

```text
链路 A：Lark / Telegram 输入 -> 需求整理 -> PRD -> 技术方案 -> 任务拆解 -> 人工确认
链路 B：Lark / Telegram 会话 -> 摘要 -> 待办 -> 风险提取 -> 项目沉淀 -> 人工确认
```

完整的长期标准链路见第七章，MVP 落地细节以第十六章为准。

### 6.4 每层的落地方式
#### 入口层实现
- Lark 通过 Bot 和消息订阅接入
- Telegram 通过 Bot API 或中转服务接入
- Web 控制台负责结构化发起任务
- 入口统一写入任务中心，而不是直接调用 Agent

#### 编排层实现
- 每个流程节点定义明确输入和输出 Schema
- 节点状态必须可持久化
- 失败必须支持重试、跳过和人工接管
- 长任务必须异步化，不能直接阻塞入口请求

#### 知识层实现
- 文档先做标准切分和去重
- 会话类数据先做摘要再入库
- 项目资料、SOP、架构文档、历史决策要分命名空间管理
- 上下文拼装要按任务类型做模板，而不是简单全文召回

#### 工具层实现
- 每个工具定义统一的输入输出协议
- 每个高风险工具必须标注 risk_level
- 每个工具必须支持 dry-run 或预检查
- 工具返回必须结构化，方便 Agent 再处理

#### 审批层实现
- 审批节点必须可配置
- 按动作类型区分审批强度
- 生产发布、主分支合并、批量修改为高风险动作
- 所有审批和执行记录必须可检索

#### 观测层实现
- 按任务、Agent、工具、模型、用户五个维度记录日志
- 每一步记录 Token 成本、执行耗时、成功率
- 对失败节点做聚类，方便优化流程

---

## 七、从想法到上线的标准链路
### 7.1 阶段 1：想法进入
输入来源：
- Lark 群聊
- Telegram 客户沟通
- 会议纪要
- 表单
- 手工提交
AI 自动动作：
- 提取需求
- 分类
- 去重
- 识别优先级
- 检索相似历史项目
输出：
- 需求摘要
- 初始卡片
- 背景资料链接

### 7.2 阶段 2：需求澄清与 PRD 生成
AI 自动动作：
- 明确目标用户
- 提炼业务价值
- 识别依赖与风险
- 生成 PRD 初稿
- 生成验收标准
输出：
- PRD
- 功能列表
- 验收清单
- 风险说明

### 7.3 阶段 3：技术方案生成
AI 自动动作：
- 检索现有系统能力
- 识别可复用模块
- 生成技术方案
- 拆成后端 / 前端 / 数据 / 测试任务
输出：
- 技术方案
- 数据模型建议
- API 设计建议
- 任务拆解

### 7.4 阶段 4：项目排期与执行
AI 自动动作：
- 生成任务树
- 标记优先级
- 分配负责人
- 生成周计划和里程碑
- 沉淀阻塞项和待办
输出：
- 项目计划
- Sprint 任务
- 跟进清单

### 7.5 阶段 5：代码与测试
AI 自动动作：
- 生成代码草稿
- 生成 SQL 和脚本
- 生成单测草稿
- 做静态检查
- 生成 PR / MR 说明
输出：
- 代码草稿
- 测试草稿
- 提交说明
- 风险清单

### 7.6 阶段 6：部署与上线
AI 自动动作：
- 生成发布清单
- 检查依赖与配置
- 生成回滚方案
- 触发测试环境部署
- 汇总验证结果
关键约束：
- 生产部署必须审批
- 风险动作必须有回滚

### 7.7 阶段 7：上线后复盘
AI 自动动作：
- 拉取监控和日志
- 拉取业务指标
- 输出复盘报告
- 识别新增问题
- 回写知识库
输出：
- 上线复盘
- 问题列表
- 后续优化建议

---

## 八、关键技术难点
### 8.1 上下文准确率
难点：
- AI 能不能拿到正确上下文，直接决定输出质量
- 如果上下文错了，PRD、方案、代码都会连锁出错
关键问题：
- 文档版本不一致
- 会议纪要和聊天记录噪声大
- 历史方案和当前实现可能不一致
应对方式：
- 做文档分层和命名空间管理
- 对聊天记录先摘要再入库
- 在重要任务里强制引用最新权威文档
- 增加“引用来源”机制，输出时附上下文出处

### 8.2 多 Agent 协作稳定性
难点：
- 多 Agent 容易出现重复劳动、角色串位、上下文污染
关键问题：
- 任务边界不清
- 中间产物格式不统一
- 一个 Agent 的错误会向后传递
应对方式：
- 为每个 Agent 定义固定职责
- 所有中间产物结构化
- 每一步增加校验器和质量门
- 关键节点引入评审 Agent 或规则检查

### 8.3 工具调用可靠性
难点：
- 真正难的不是“能生成文本”，而是“能稳定调用工具做事”
关键问题：
- 外部 API 不稳定
- 权限不足
- 参数错误
- 执行动作不可回滚
应对方式：
- 所有工具都要封装成标准化服务
- 所有高风险动作先 dry-run
- 对外部系统增加重试、熔断和降级
- 保留人工接管入口

### 8.4 审批与自动执行边界
难点：
- 自动化越强，越需要明确边界
关键问题：
- 哪些动作能自动执行
- 哪些动作必须人工审批
- 谁对最终结果负责
应对方式：
- 对动作做风险分级
- 设白名单动作
- 生产环境和批量动作默认审批
- 低风险动作逐步放权

### 8.5 成本控制
难点：
- 多 Agent、多步骤、多模型会迅速放大 Token 成本和调用复杂度
关键问题：
- 上下文过长
- 重复调用
- 对简单任务使用了高成本模型
应对方式：
- 模型分层路由
- 上下文裁剪和缓存
- 批量任务异步处理
- 关键任务和普通任务分档

### 8.6 代码与发布安全
难点：
- AI 生成代码和自动发布一旦缺少约束，风险非常高
关键问题：
- 生成错误代码
- 覆盖旧逻辑
- 环境配置错误
- 回滚不完整
应对方式：
- 代码生成必须经过静态检查和测试
- 发布前必须生成检查清单和回滚方案
- 生产部署必须审批
- 所有变更都要留审计记录

### 8.7 数据不完整下的自动化能力上限
难点：
- 当前媒体侧和业务侧数据尚未稳定打通，会限制某些高阶自动化能力
直接影响：
- 无法稳定做完整闭环优化
- 无法稳定做高精度自动预算优化
- 无法完全依赖实时媒体数据做强决策
应对方式：
- 先做流程自动化和知识自动化
- 先做建议型系统，再做执行型系统
- 接受半自动数据入口作为过渡方案

---

## 九、关键技术依赖
### 9.1 平台级依赖
- Lark Open Platform
- Telegram Bot API
- GitHub / GitLab API
- CI/CD 系统
- 部署系统
- PostgreSQL
- Redis
- 对象存储
- 向量检索服务

### 9.2 模型级依赖
- 高质量通用模型
- 低成本摘要 / 分类模型
- 多模态模型
- 模型路由与缓存层

### 9.3 工程级依赖
- 标准化任务状态机
- 审批流能力
- 权限系统
- 审计日志系统
- 可回滚发布机制
- 统一配置中心

### 9.4 知识级依赖
- 公司战略和背景文档
- 产品文档和技术文档
- 会议纪要和项目资料
- 代码仓库可检索能力
- SOP 和流程文档

### 9.5 当前最关键的前置依赖
如果只看 AZSL 当前阶段，最关键的依赖不是更多模型，而是：
- 统一的知识沉淀方式
- 统一的任务和审批体系
- 统一的工具接入协议
- 统一的日志和审计体系

---

## 十、实现成本与关键资源评估
### 10.1 成本判断的总体结论
这类“从想法到上线”的 AI 全流程自动化系统，真正昂贵的往往不是模型本身，而是：
- 流程梳理与治理
- 工具接入与标准化
- 审批、审计与回滚体系
- 知识沉淀质量
- 平台稳定性建设
对 AZSL 当前阶段，更适合先做一个可运行、可扩展、可治理的 MVP，而不是直接建设重型平台。
本章中的月度成本估算默认主要指平台运行相关成本，不包含正式全职人员工资和长期组织管理成本。

### 10.2 成本拆分方式
建议把成本拆成四类：
| 成本类别 | 主要内容 |
|-----|---------|
| **建设成本** | 人力开发、流程设计、知识清洗、工具接入 |
| **运行成本** | Token、服务器、存储、向量检索、日志监控 |
| **治理成本** | 权限、审批、审计、回滚、运维值守 |
| **隐性成本** | 上下文错误、流程边界不清、自动化返工、知识失真 |

### 10.3 三个阶段的成本判断
#### MVP 阶段
目标：
- 跑通想法到 PRD
- 跑通 PRD 到技术方案和任务拆解
- 跑通沟通摘要、待办沉淀
- 跑通代码草稿和 PR 草稿
资源特征：
- 以外部模型 API 为主
- 以轻量工作流和知识库为主
- 不追求复杂闭环自动执行
非人力月运行成本粗估：
- **$500 - $2,000 / 月**

#### 标准平台阶段
目标：
- 多 Agent 编排
- 审批流
- Tool Hub
- 审计与观测
- 更稳定的 CI/CD 对接
非人力月运行成本粗估：
- **$2,000 - $6,000 / 月**

#### 公司级平台阶段
目标：
- 多团队并发
- 更高自动化比例
- 完整治理和复盘
- 更细粒度成本控制
非人力月运行成本粗估：
- **$5,000 - $15,000+ / 月**

### 10.4 最大成本项判断
对这类系统，通常成本占比从高到低大致是：
1. **人力成本**
2. **模型 / Token 成本**
3. **服务器与存储成本**
4. **监控、日志、向量检索等配套成本**
因此前期不应把关注点全部放在服务器上，更关键的是：
- 人员是否匹配
- 流程边界是否清晰
- 文档和知识是否可用
- 工具接入是否统一

### 10.5 关键人力资源
最关键的不是人数很多，而是角色齐全。
建议核心角色：
- **平台 / 后端负责人**
- **工作流 / Agent 工程师**
- **知识库 / 数据工程师**
- **前端 / 控制台支持**
- **产品 / 业务 Owner**
- **运维 / DevOps 支持**

#### 最小配置
- 1 名平台 / 后端
- 1 名数据 / 知识工程师
- 0.5 到 1 名前端
- 1 名产品 / 业务 owner
- 0.5 名运维支持
这个配置适合：
- 跑通 MVP
- 打通 1 到 2 条主链路

#### 推荐配置
- 1 名平台 / 后端负责人
- 1 名工作流 / Agent 工程师
- 1 名数据 / 知识工程师
- 1 名前端
- 1 名产品 / 业务 owner
- 0.5 到 1 名运维支持
这个配置适合：
- 建设稳定平台
- 并行推进 2 到 3 条自动化链路

### 10.6 系统资源需求
如果前期主要使用外部模型 API，则不建议一开始采购 GPU 服务器。
启动期推荐资源：
| 组件 | 建议配置 | 用途 |
|-----|---------|------|
| **AI Gateway** | 2C4G × 1 | 模型路由、缓存、成本统计 |
| **Workflow Worker** | 4C8G × 1 | 多 Agent 任务编排与异步执行 |
| **Redis** | 1C2G × 1 | 队列、缓存、会话状态 |
| **PostgreSQL** | 托管优先 | 状态、审批、任务、元数据 |
| **向量检索** | pgvector 或 Qdrant 小实例 | 知识库召回 |
| **对象存储** | 小规模即可 | 文档、日志、产物归档 |
| **日志监控** | 轻量起步 | 观测、诊断、审计 |
服务器月度成本粗估：
- **启动期**：$200 - $800 / 月
- **标准运行**：$800 - $2,000 / 月
- **复杂平台**：$2,000 - $5,000 / 月

#### 10.6.1 按当前 MVP + LangGraph 方案的资源判断

如果当前只做第十六章定义的两条 MVP 主链路，并且模型仍然走外部 API，那么：

- **不需要 GPU**
- **不需要 Kubernetes**
- **不需要专门的大规模工作流集群**
- **LangGraph 本身不会成为设备成本大头**

当前 MVP 的主要资源压力并不在 LangGraph，而在：

1. 模型 API 调用次数
2. 沟通记录和知识检索带来的上下文处理
3. 异步任务执行时的并发控制
4. 日志与审计留存

也就是说，LangGraph 在 MVP 阶段更像“流程控制器”，不是“吃机器的大户”。

#### 10.6.2 更贴近 MVP 的推荐部署

如果想进一步压低成本，MVP 阶段推荐两种部署方式：

**方案 A：最省成本**

- 1 台 `4C8G` 应用机
  - 同时承载 API Gateway、LangGraph Worker、轻量审批和任务服务
- 1 个托管 `PostgreSQL`
- 1 个托管 `Redis`
- 1 个对象存储

适用前提：

- 同时在线用户不多
- 每日工作流执行量较低
- 以内部试点和小范围团队使用为主

**方案 B：更稳一些**

- 1 台 `2C4G` 应用机，负责 API Gateway / Bot 接入
- 1 台 `4C8G` Worker 机，负责 LangGraph 编排和异步任务
- 1 个托管 `PostgreSQL`
- 1 个托管 `Redis`
- 1 个对象存储

适用前提：

- 需要把入口流量和后台编排隔离
- 需要降低长任务对入口响应的影响
- 需要更稳定地做失败重试和异步执行

#### 10.6.3 对当前 MVP 的成本结论

如果采用 `FastAPI + LangGraph + PostgreSQL + Redis + 外部模型 API`：

- **设备资源成本是可控的**
- **不会因为用了 LangGraph 就明显变重**
- **前期真正的大头仍然是人力和 Token，不是服务器**

因此，当前是否使用 LangGraph，应该主要从“流程是否需要可恢复、可审计、可人工接管”判断，而不是从“机器会不会太贵”判断。

#### 10.6.4 MVP 存储量预估与建议结论

对当前这版 MVP，不建议一开始按 TB 级规划，更适合按 `PostgreSQL + Redis + 对象存储 + 向量检索 + 日志` 五类来估。

推荐用这个简化公式：

`总存储量 = 结构化数据 + 原始文件 + 向量索引 + 日志 + 2x~3x 预留冗余`

按当前较保守的假设：

- 每天 `100` 个工作流
- 每个工作流平均 `8` 个步骤
- 每天新增或导出约 `200MB` 文件
- 初期知识库原始文档约 `2GB`
- 日志和观测数据约 `300MB / 天`

更合理的结论是：

- **PostgreSQL 前期通常不到 `1GB`**
- **主要空间消耗在对象存储、日志和向量检索**
- **第一阶段有效使用量大致在 `30GB ~ 60GB`**
- **建议总规划容量按 `100GB` 以内启动**

推荐起始规划如下：

| 存储组件 | 建议起始规划 |
|-----|---------|
| **PostgreSQL** | `20GB` |
| **Redis** | `1GB ~ 4GB` 内存 |
| **对象存储** | `50GB` |
| **向量检索** | `10GB` |
| **日志与观测** | `20GB ~ 50GB` |

控制原则建议只记住 4 条：

- `PostgreSQL` 只存结构化状态和索引，不存长篇原文
- 原文、导出物、附件统一进对象存储
- 日志按热日志、审计日志、归档分级保留
- 向量库只收需要检索的知识，避免把所有聊天原文都向量化

如果后续真实工作流量明显高于预期，再按两周或一月周期复盘和扩容，会比一开始按大平台思路超前准备更合理。

### 10.7 Token 成本评估
全流程自动化里的 Token 成本主要来自：
- 想法整理与 PRD 生成
- 技术方案与任务拆解
- 沟通记录摘要与知识沉淀
- 代码草稿与测试草稿
- 复盘总结与问答
按当前阶段估算：
- **MVP**：$300 - $1,500 / 月
- **标准平台**：$1,500 - $5,000 / 月
- **重度使用**：$5,000+ / 月

### 10.8 Token 成本失控的常见原因
- 上下文过长
- 同一任务反复多轮调用
- 对简单任务使用高价模型
- 聊天记录不做摘要直接全量输入
- 代码修改链路重复反刍

### 10.9 成本控制建议
- 分类、提取、摘要用低成本模型
- 关键生成任务再调用高质量模型
- 对重复上下文做缓存
- 对沟通记录先摘要再入库
- 让多 Agent 共用中间产物，避免重复调用
- 对每个 Agent 单独记账和限额
- 对任务设成本阈值和告警

### 10.10 关键资源判断
如果只看当前最关键资源，优先级大致如下：
1. **知识沉淀机制**
2. **任务和审批体系**
3. **工具接入协议**
4. **日志与审计体系**
5. **稳定的产品 / 业务 owner**
6. **基础数据库与状态管理能力**
7. **模型路由与缓存能力**
也就是说，当前最稀缺的资源并不是 GPU，而是：
- 可治理的流程
- 结构化的知识
- 可复用的工具接口
- 稳定的 owner 机制

### 10.11 对 AZSL 当前阶段的建议
当前更适合：
- 先做 1 到 2 条高价值主链路
- 先用外部模型 API
- 先建设轻量中台
- 先做审批式自动化
- 先接受“AI 主导 + 人工确认”的模式
不建议当前阶段：
- 直接建设重型平台
- 直接追求完全无人值守
- 在知识、审批、工具协议未统一前扩大自动执行

---

## 十一、最适合 AZSL 的实现方式
本章侧重“整体落地方案怎么选”；与后面的“关键技术栈建议”是互补关系，前者强调方案组合，后者强调技术清单。

### 11.1 方案 A：轻量生产可用
适合当前阶段快速落地。
推荐组合：
- FastAPI
- LangGraph
- PostgreSQL
- Redis
- Qdrant 或 pgvector
- Lark Bot
- Telegram Bot
- GitHub / GitLab API
- CI/CD
- OpenAI / Claude / Qwen
适合原因：
- 架构轻
- 成本可控
- 适合 10 到 15 人产研团队
- 容易先跑通 1 到 2 条流程

### 11.2 方案 B：更企业级
适合后续升级成公司级平台。
推荐组合：
- FastAPI
- Temporal
- PostgreSQL
- Redis / Kafka
- Qdrant
- ClickHouse
- Lark Bot / Telegram Bot
- GitLab / CI/CD / ArgoCD
- 多模型路由层
适合原因：
- 更适合复杂工作流
- 更强的可恢复和可审计能力
- 更适合多 Agent 并行和任务重试

### 11.3 推荐结论
对你们当前阶段，更推荐：
- 先用 **方案 A**
- 跑通后再演进到 **方案 B**

---

## 十二、优先自动化顺序
不建议一口气自动化全流程。当前阶段建议按以下顺序推进：

1. **想法到任务**
   - 价值最高，最容易验证是否真能减少 PRD、方案和任务拆解的人力消耗
2. **沟通到沉淀**
   - 直接解决 Lark / Telegram 沟通散落问题，最适合先落地
3. **代码草稿到 PR**
   - 对研发效率提升明显，但必须保留人工 review
4. **发布准备与复盘**
   - 适合在前几段稳定后再补，不建议过早做成自动闭环

MVP 具体范围和落地方式以第十六章为准，本章只保留优先级结论。

---

## 十三、关键技术栈建议
本章按技术类别汇总推荐栈，便于实施时做选型清单；不替代前文的方案 A / 方案 B 组合判断。

### 13.1 当前阶段的技术栈汇总

为避免和前文“方案 A / 方案 B”以及后文“分层对比”重复，这里只保留一份压缩版汇总：

| 技术层 | 当前推荐 |
|-----|---------|
| **入口与交互** | Lark Bot、Telegram Bot、Web 控制台、API Gateway |
| **编排与调度** | LangGraph；长流程升级到 Temporal；异步任务用队列 |
| **存储与状态** | PostgreSQL、Redis、对象存储、ClickHouse |
| **知识与检索** | pgvector 起步，规模上来后再评估 Qdrant |
| **工具与执行** | GitHub / GitLab API、CI/CD、Lark Open Platform、Telegram Bot API |
| **模型层** | 高质量模型处理关键生成，经济型模型处理提取、分类、摘要 |
| **观测与审计** | OpenTelemetry、Grafana、日志系统、成本统计系统 |

如果要做具体选型判断，以本章后面的模块接口和分层方案对比为主。

### 13.2 模块接口清单
为了让系统真正可实施，建议至少先定义以下模块接口，而不是只停留在概念架构层。
| 模块 | 关键接口 / 能力 | 说明 |
|-----|---------------|------|
| **AI Gateway** | `chat_completion` / `embedding` / `rerank` / `cost_report` | 统一模型调用、缓存、限流、成本统计 |
| **Workflow Orchestrator** | `start_workflow` / `resume_workflow` / `retry_step` / `handoff_to_human` | 负责任务状态机、人工接管、失败重试 |
| **Knowledge Hub** | `ingest_doc` / `search_context` / `write_summary` / `link_reference` | 负责文档接入、检索、摘要沉淀和引用 |
| **Tool Hub** | `run_git_action` / `create_task` / `send_lark_message` / `trigger_ci` / `deploy_preview` | 把外部系统能力封装成标准 Tool |
| **Approval & Audit** | `submit_approval` / `approve_action` / `reject_action` / `query_audit_log` | 管理审批流、操作记录和责任追踪 |
| **Ops Console** | `list_workflows` / `view_run_detail` / `view_cost` / `view_failure` | 提供运维、审计、复盘和人工干预入口 |
接口设计建议：
- 所有接口统一使用结构化输入输出
- 所有高风险动作都返回 `risk_level`
- 所有执行型接口都应支持 `dry_run`
- 所有关键接口都要带 `trace_id` 便于审计和排障

### 13.3 当前阶段的推荐组合

为避免和第十一章“方案 A / 方案 B”重复，这里不再展开长篇逐层对比，只保留当前最推荐的组合结论：

- **入口层**：Lark Bot + Telegram Bot
- **编排层**：LangGraph 为主，低风险支线可按需补 Dify / n8n
- **知识层**：PostgreSQL + pgvector，规模上来后再评估 Qdrant
- **工具层**：FastAPI Tool Hub + GitHub Actions / GitLab CI
- **审批层**：Lark 审批入口 + GitHub / GitLab 原生研发审批
- **观测层**：OpenTelemetry + Grafana + Langfuse

之所以这样选，核心原因只有 4 个：

- 符合当前 `Lark + Telegram` 的协作现实
- 符合当前 `10 到 15 人` 团队的可承载范围
- 符合“先半自动、再自动化”的节奏
- 符合“先跑通 1 到 2 条主链路，再逐层升级”的资源优先级

---

## 十四、AI 角色设计建议
Agent 角色和职责边界已经在 [5.2](file:///Users/azsl/AITRIKB/ai-full-process-automation-technical-plan.md#L104-L123) 说明，这里不再重复展开。

当前阶段建议坚持两个原则：

- 不要用一个“大而全”的总控 Agent 覆盖全部职责
- 先把需求、PRD、方案、PM、研发、测试、发布、复盘这些角色边界做清楚，再讨论更复杂的多 Agent 协作

---

## 十五、风险与约束
### 15.1 当前现实约束
- 媒体平台侧与业务侧数据尚未稳定打通
- Meta API 系统用户 Token 当前不稳定
- 团队规模有限，不适合同时建设过多重系统
- 对外与对内协作工具分散，容易信息断层

### 15.2 关键风险
- 无审批直接执行高风险动作
- 自动化质量不稳导致错误扩散
- 上下文不准导致错误方案和错误代码
- Token 成本失控
- 过早追求闭环自动化

### 15.3 风险控制建议
- 高风险动作必须审批
- 所有执行动作记录审计日志
- 所有自动修改都支持回滚
- 先小范围白名单试点
- 先辅助决策，再逐步自动执行

### 15.4 审批动作分级表
建议把自动化动作按风险分为四级：
| 风险级别 | 典型动作 | 默认策略 |
|-----|---------|---------|
| **L1 低风险** | 摘要生成、文档整理、知识回写、任务草稿 | 可自动执行，保留日志 |
| **L2 中低风险** | PRD 初稿、技术方案初稿、任务拆解、测试草稿 | 可自动生成，但需人工 review |
| **L3 中高风险** | 建立 PR、修改配置、触发测试环境部署、批量消息回写 | 需审批或白名单授权 |
| **L4 高风险** | 合并主分支、生产发布、批量代码修改、批量删除或覆盖动作 | 默认禁止自动执行，必须人工审批和回滚预案 |
审批设计建议：
- L1 动作默认自动执行
- L2 动作默认“自动生成 + 人工确认”
- L3 动作默认进入审批流
- L4 动作默认强审批，且必须有回滚说明、责任人和审计记录

---

## 十六、实施路线建议
### 16.1 第一阶段：AI 辅助型
目标：
- 跑通想法整理
- 跑通 PRD 生成
- 跑通会议摘要和项目沉淀

### 16.2 第二阶段：AI 执行型
目标：
- 跑通任务拆解
- 跑通代码草稿
- 跑通测试草稿
- 跑通 PR 自动生成

### 16.3 第三阶段：AI 编排型
目标：
- 多 Agent 协同
- 流程自动流转
- 审批与回滚打通
- 上线后自动复盘

### 16.4 MVP 范围边界
为了避免第一版范围失控，建议明确 MVP 只做以下内容：

#### MVP 建议纳入范围
- 想法整理为结构化需求
- PRD 初稿生成
- 技术方案初稿生成
- 任务拆解与里程碑草稿
- Lark / Telegram 沟通摘要与项目沉淀
- 代码草稿、测试草稿、PR 草稿
- 发布前检查清单和回滚方案草稿

#### MVP 暂不纳入范围
- 无审批的生产发布
- 高风险批量代码修改
- 强依赖媒体实时数据的自动优化闭环
- 完整自动预算优化与自动调价
- 没有审计的跨系统自动执行
- 复杂多模态大规模内容生产流水线

#### MVP 验收标准建议
- 能稳定跑通至少 1 条“想法 → PRD → 方案 → 任务”的链路
- 能稳定跑通至少 1 条“沟通 → 摘要 → 待办 → 项目沉淀”的链路
- 能稳定生成代码草稿和 PR 草稿
- 关键执行动作具备审批、日志和人工接管能力
- 能看见成本、失败节点和人工介入点

### 16.5 MVP 落地方案（推荐直接按这个做）

如果现在就要启动 MVP，建议不要从“全流程平台”视角切入，而是从“先打通 2 条高价值链路”切入。

#### 16.5.1 MVP 目标

MVP 的目标不是替代整个产研流程，而是验证以下 4 件事：

1. AI 是否能稳定把非结构化输入整理成结构化产出
2. AI 是否能减少 PRD、方案、任务拆解和沟通沉淀的人力消耗
3. AI 驱动的低风险执行链路是否可控、可审计
4. 在当前团队规模下，这套系统是否值得继续扩建

#### 16.5.2 MVP 只做哪 2 条主链路

建议先只做以下两条：

**链路 A：想法到任务**

```text
Lark / Telegram 输入
-> 需求整理
-> PRD 初稿
-> 技术方案初稿
-> 任务拆解
-> 人工确认
-> 写入项目系统
```

**链路 B：沟通到沉淀**

```text
Lark / Telegram 会话
-> 摘要生成
-> 待办提取
-> 风险点提取
-> 项目知识沉淀
-> 人工确认
-> 写回知识库 / 项目记录
```

这两条链路的共同特点是：

- 高频
- 低风险
- 容易验证价值
- 不依赖复杂生产执行

#### 16.5.3 MVP 暂时不要做什么

MVP 阶段明确不做：

- 多 Agent 超复杂协同状态机
- 无审批自动发版
- 高风险跨系统自动执行
- 重型 Web 控制台
- 很重的企业级权限体系
- 强依赖实时媒体数据的自动优化

#### 16.5.4 MVP 推荐系统形态

MVP 建议采用“轻量 5 件套”：

1. **Lark / Telegram Bot**
   - 作为输入入口
2. **AI Gateway**
   - 负责模型路由、Prompt 模板、成本统计、缓存
3. **Workflow Orchestrator**
   - 负责编排 2 条主链路
4. **Knowledge Hub**
   - 负责知识检索、摘要沉淀、历史记录归档
5. **Approval & Audit**
   - 负责人工确认、执行日志、失败回放

如果资源紧张，`Approval & Audit` 可以先内聚在主服务中，不需要单独拆服务。

#### 16.5.5 MVP 推荐技术方案

对当前阶段，更推荐轻量生产可用方案：

- 后端：`FastAPI`
- 编排：`LangGraph`
- 数据库：`PostgreSQL`
- 缓存：`Redis`
- 向量检索：`pgvector`
- 模型：外部模型 API（OpenAI / Claude / Qwen 等）
- 入口：`Lark Bot + Telegram Bot`
- 项目侧集成：项目管理系统 API / Git API

为什么这样选：

- 搭建快
- 成本低
- 方便先打通主链路
- 后续可演进，不会太早锁死

#### 16.5.6 MVP 一级模块清单

MVP 第一版只保留以下一级模块：

1. 输入接入模块
2. 任务中心模块
3. Prompt 与模型调用模块
4. 工作流编排模块
5. 知识检索与沉淀模块
6. 审批与日志模块

不建议第一版就做独立的：

- 多租户控制台
- 复杂运营看板
- 大而全 Tool Hub
- 复杂权限平台

#### 16.5.7 MVP 需要接哪些工具

建议只接最有价值、最容易验证的工具：

- Lark
- Telegram
- Git 仓库
- 项目管理系统
- 文档/知识库系统

第一版不建议接太多工具，否则联调和治理成本会迅速上升。

#### 16.5.8 MVP 人力建议

最小配置建议：

- 1 名后端 / 平台负责人
- 1 名数据 / 知识工程师
- 0.5 到 1 名前端（如有简单控制页）
- 1 名产品 / 业务 Owner
- 0.5 名运维支持

如果必须更精简，也可以采用：

- 1 名全栈
- 1 名产品 / 业务 Owner
- 0.5 名知识整理 / 运营协同

但这种模式更适合试点，不适合同时推进多条链路。

#### 16.5.9 MVP 时间建议

如果目标是“可运行版本”，比较现实的节奏如下：

**第 1 周**

- 确认 2 条主链路
- 定义输入输出 Schema
- 整理 Prompt 模板
- 准备最小知识集

**第 2 周**

- 接通 Lark / Telegram
- 接通 AI Gateway
- 跑通需求整理、PRD、摘要链路

**第 3 周**

- 跑通任务拆解和项目沉淀
- 加入人工审批与执行日志
- 接通项目系统或 Git 基础动作

**第 4 周**

- 灰度试点
- 补失败重试
- 补成本看板
- 跑首次复盘

也就是说，MVP 不追求“大而全”，而是争取在 3 到 4 周内跑出第一版可验证流程。

#### 16.5.10 MVP 验收结论标准

是否继续投入，不建议凭主观感觉判断，而建议看以下结果：

- 需求整理和 PRD 生成是否明显节省时间
- 摘要与待办提取是否稳定可用
- 人工 review 成本是否可接受
- 失败率是否在可控范围
- Token 成本是否在预算内
- 业务方是否愿意继续在真实流程中使用

如果这几个点不能成立，就不建议过早进入平台化阶段。

---

## 十七、最终建议
对 AZSL 当前阶段，最合适的方案不是“一个超级 AI 包打天下”，而是建设一套：
- 以 Lark / Telegram 为入口
- 以 Agent 编排引擎为核心
- 以知识库和项目系统为上下文底座
- 以标准化工具执行为手脚
- 以审批、审计、回滚为安全边界
一句话概括：
> 先把“想法整理、方案生成、任务拆解、沟通沉淀、代码草稿、发布准备”这几段做成 AI 主导流程，再逐步升级为从想法到上线的全流程自动化平台。