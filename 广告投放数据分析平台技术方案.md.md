# 广告投放数据分析平台技术方案

> **适用公司**：AZSL / 艾泽 / AITRI  
> **业务场景**：广告投放服务、BI 平台、自动化投放、AI 投放能力建设  
> **当前重点**：在事件采集已切到 ClickHouse 的基础上，建设以 ClickHouse 为核心的分析底座，先支撑 Meta 提效，再扩展多渠道，再沉淀算法能力  
> **文档定位**：作为广告投放数据分析平台的技术参考方案

---

## 一、背景与目标

### 1.1 背景

公司当前服务对象以印度、巴基斯坦、东南亚的真金类游戏运营公司为主，也可能覆盖数字货币行业客户，后续可扩展到更多行业。  
核心诉求不是单纯做报表，而是通过数据能力支撑以下目标：

- 提升广告投放效率
- 提升广告投放效果
- 沉淀可复用的数据资产
- 支撑自动化投放能力
- 为后续 DMP、DSP、AI 投放能力提供底座

### 1.2 技术目标

广告投放数据分析平台需要解决以下问题：

- 把广告平台、归因平台、业务转化数据统一起来
- 建立统一指标口径，避免各团队各算各的
- 支撑账户、Campaign、素材、国家、时间等多维分析
- 支撑日报、周报、看板、异常监控和诊断
- 为自动调价、预算优化、素材分析、风控预测提供可复用数据

补充边界说明：

- `API-uevent.md` 是面向业务方、游戏侧、落地页侧的统一事件上报协议
- 分析平台不直接对外开放业务事件写入协议，而是消费事件采集系统按该协议落入 `ClickHouse raw` 的结果

### 1.3 建设原则

| 原则 | 说明 |
|-----|------|
| **业务结果优先** | 先支撑消耗、转化、ROI、ROAS 等核心业务指标 |
| **轻中台优先** | 启动期不做重型大数据平台，先做可迭代的轻中台 |
| **分层建模** | 原始数据、标准明细、分析服务分层，保证口径稳定 |
| **多渠道可扩展** | 先打透 Meta，后续可平滑接入 Google、TikTok 等 |
| **平台化沉淀** | 分析平台不是临时报表系统，而是自动化和算法能力底座 |

---

## 二、总体方案结论

### 2.1 推荐技术栈

当前阶段最推荐的组合：

- **Python**：外部数据接入、调度脚本、质量校验、规则计算、算法原型
- **PostgreSQL**：业务配置、租户信息、任务状态、权限与元数据
- **ClickHouse**：事件事实主底座、标准明细层、主题汇总层、应用宽表层
- **对象存储（S3 / MinIO）**：原始数据归档、回溯、备份
- **Airflow 或 Dagster**：定时调度、依赖编排、失败重跑
- **Metabase**：快速搭建 BI 看板与自助分析能力
- **FastAPI**：为内部平台提供指标查询、诊断分析、策略接口

### 2.2 方案定位

这套方案适合：

- 需要先快速搭建数据分析底座
- 当前以 Meta 为主要渠道，但明确会走向多渠道
- 未来要把 BI、自动化投放、AI 能力串成一条链路
- 需要兼顾成本、迭代速度和可扩展性
- 当前事件采集主存已经迁移到 ClickHouse，希望后续分析链路尽量在 ClickHouse 内完成

不建议当前阶段直接采用：

- Hadoop / Spark 全家桶
- 纯实时流式大数据平台
- 一上来就完全自研 BI 前端

原因是这些方案前期投入过大，不适合以业务提效和数据沉淀为第一目标的阶段。

---

## 三、整体架构设计

### 3.1 架构概览

```text
【广告平台】
Meta / Google / TikTok / 其他渠道
        │
        ├── API 拉取广告数据
        │
【业务与归因平台】
游戏注册 / 付费 / 留存 / ThinkingData / AppsFlyer / Adjust
        │
        ├── API / 数据库 / 文件同步
        ▼
【接入与落库层】
Python Connectors / 事件采集系统
        │
        ├── 原始事件与明细落 ClickHouse Raw
        ├── 原始文件归档到对象存储
        ▼
【ClickHouse 内部计算层】
标准化清洗 / 维度映射 / 币种与时区统一 / 指标计算
        ├── DWD 标准事实表
        ├── DWS 主题汇总表
        ├── ADS 应用宽表
        ▼
【服务与配置层】
ClickHouse：事件主底座 + 分析主库
PostgreSQL：配置、权限、任务状态、元数据
对象存储：归档、回放、补数文件
        ▼
【服务与展示层】
Metabase / 自研 BI 控制台 / FastAPI
        ▼
【上层能力】
日报周报 / 异常监控 / 自动诊断 / DMP / 营销中台 / 自动化投放 / AI 优化
```

### 3.2 分层设计

| 层级 | 作用 | 推荐存储 |
|-----|------|---------|
| **Raw / ODS** | 保存广告 API、业务系统、事件采集系统的原始数据，便于回放与排错 | ClickHouse + 对象存储 |
| **DWD** | 标准化事实层，统一字段、时间、币种、国家、账户映射与事件口径 | ClickHouse |
| **DWS** | 面向主题分析的汇总层，沉淀日报、小时报、漏斗、留存、LTV、回收等结果 | ClickHouse |
| **ADS** | 面向 BI、DMP、营销中台、自动化策略平台和接口层的应用宽表 | ClickHouse |
| **App / API** | 对内提供查询、看板、异常、策略接口 | FastAPI + Metabase |

建议当前阶段明确一个原则：

- `ClickHouse` 是事件事实主底座和分析主引擎
- `PostgreSQL` 不再承担大规模事件存储，只承担配置、权限、任务、元数据
- `对象存储` 只负责归档、回放和补数文件，不作为高频分析主查询入口

### 3.3 推荐数据链路

当前更推荐的不是“外部 ETL 做完大部分转换后再写 CH”，而是采用 **CH-centric ELT**：

1. 外部数据先进入 `ClickHouse raw`
2. 在 CH 内完成标准化和事实层构建
3. 在 CH 内完成主题汇总和应用宽表构建
4. BI、DMP、营销中台、自动化策略统一消费 `DWS / ADS`

推荐实现方式：

- `实时或准实时链路`
  - 优先用 `Materialized View`
  - 适合原始事件到标准事实表
- `复杂口径和可重算链路`
  - 优先用调度 SQL
  - 适合留存、LTV、回收、异常检测、归因修正
- `面向业务直接消费`
  - 优先落 `ADS` 表，不建议每次临时扫超大明细

### 3.4 数据边界

平台建议只聚焦与投放效果直接相关的数据：

- 广告平台消耗数据
- 点击、展现、转化、素材表现数据
- 注册、付费、回收、留存等业务结果数据
- 账户、BM、Campaign、素材、国家、时间等维度数据

对高敏感个人数据不建议在早期大范围进入分析平台，优先保留：

- 用户匿名标识
- 归因结果
- 聚合业务结果

同时建议明确边界：

- `API-uevent.md` 负责定义“事件怎么上报进来”
- 事件采集系统负责“事件怎么被接收、校验、落库和回放”
- 分析平台负责“事件怎么被标准化、聚合、计算和服务化输出”

---

## 四、核心业务对象与指标体系

### 4.1 核心分析对象

- 渠道：Meta、Google、TikTok 等
- 组织：BM、广告账户、项目、客户、租户
- 投放结构：Campaign、Ad Set、Ad、Creative
- 业务维度：国家、地区、语言、设备、系统、产品
- 时间维度：小时、日、周、月

### 4.2 第一阶段必须统一的指标

| 指标分类 | 核心指标 |
|-----|------|
| **消耗类** | spend, impressions, clicks, CPM, CPC, CTR |
| **转化类** | installs, register, first_deposit, purchase, CVR, CPA |
| **收益类** | revenue, ROAS, ROI, payback_days, LTV |
| **结构类** | campaign_count, adset_count, ad_count, creative_count |
| **质量类** | 素材首日表现、衰减速度、异常波动、转化回传完整率 |

### 4.3 指标统一要求

- 指标口径必须唯一
- 原始数据与汇总指标可追溯
- 币种必须统一
- 时区必须统一
- 国家、账户、素材等维度必须标准化
- 所有报表和接口优先复用统一口径，而不是临时计算

---

## 五、推荐技术选型说明

### 5.1 为什么用 Python

- 广告平台 API 接入生态成熟
- 适合外部接入、规则引擎、异常检测、算法原型
- 适合后续衔接自动化投放和 AI 能力
- 当前更适合作为 `接入层与调度层`，而不是承担主要分析存储

### 5.2 为什么用 ClickHouse

广告投放分析天然属于高维聚合分析场景，ClickHouse 适合：

- 大量事件与明细数据写入
- 国家、渠道、Campaign、素材等多维切片
- 小时级、天级、周级快速聚合
- 指标查询响应快
- 当前已经承接事件采集主存，更适合作为统一分析底座继续向上演进

更推荐的使用方式不是把 ClickHouse 当普通 OLTP 数据库，而是围绕以下模型建设：

- **原始事件表**：保存事件采集系统和外部接入的原始事件
- **标准事实表**：保存广告明细、转化事件、付费事件、归因结果
- **主题汇总表**：保存小时级、天级、国家级、素材级聚合结果
- **应用宽表**：给 BI、报表、DMP、营销中台和自动化平台直接查询
- **物化视图**：把高频聚合计算前置，降低实时查询成本
- **标签 / 分层结果表**：保存用户分层、异常检测、策略结果

### 5.3 为什么保留 PostgreSQL

PostgreSQL 更适合管理分析平台的业务型数据：

- 客户与租户信息
- BM / 广告账户映射
- 任务状态与调度记录
- 权限与菜单配置
- 指标定义与元数据

不建议继续把 PostgreSQL 当事件存储或分析副本使用。

### 5.4 为什么先用 Metabase

- 快速出报表
- 研发成本低
- 方便运营、投手、管理层先统一看数
- 后续等指标体系稳定后，再补自研 BI 控制台

---

## 六、数据模型建议

### 6.1 推荐主题表

建议至少建设以下主题：

- 广告消耗主题
- 转化回传主题
- 素材表现主题
- 国家与区域主题
- 账户与组织主题
- 客户与项目主题
- 收益与回收主题
- 异常监控主题

### 6.2 推荐核心事实表

| 表类型 | 示例 |
|-----|------|
| **广告事实表** | fact_ads_performance_daily |
| **素材事实表** | fact_creative_performance_daily |
| **转化事实表** | fact_conversion_daily |
| **收益事实表** | fact_revenue_daily |
| **小时事实表** | fact_ads_performance_hourly |
| **异常事实表** | fact_anomaly_event |

### 6.3 推荐核心维表

| 维表类型 | 示例 |
|-----|------|
| **渠道维表** | dim_channel |
| **客户维表** | dim_client |
| **项目维表** | dim_project |
| **账户维表** | dim_ad_account |
| **Campaign 维表** | dim_campaign |
| **素材维表** | dim_creative |
| **国家维表** | dim_country |
| **时间维表** | dim_date, dim_hour |

### 6.4 基于 ClickHouse 的分析建模建议

建议把 ClickHouse 中的数据分析分成 5 类表：

- **原始事件表**：如 `uevent` 原始上报、媒体原始回传、业务原始结果
- **标准事实表**：如点击、激活、注册、付费、回传、异常事件
- **聚合事实表**：如天级广告表现、小时级素材表现、国家级转化表现
- **结果宽表**：如日报宽表、客户看板宽表、自动化投放输入表
- **标签结果表**：如用户分层、流失标记、高价值标记、异常标记

推荐模式：

- 高频写入数据先进入原始事件表
- 原始事件优先通过物化视图进入标准事实表
- 复杂口径通过调度 SQL 汇总到主题表
- BI 和 API 优先查聚合表或宽表，不直接扫超大原始表
- 用户级配置、权限、任务状态仍放在 PostgreSQL，不放在 ClickHouse

### 6.5 推荐数据库分层

建议在 ClickHouse 内至少按下面方式组织：

- `raw`
  - 保存原始事件、外部平台拉取明细、补数中间表
- `dwd`
  - 保存标准事件事实表、标准广告事实表、标准转化事实表
- `dws`
  - 保存小时级、日级、国家级、素材级、渠道级主题汇总
- `ads`
  - 保存日报宽表、客户宽表、自动化输入表、诊断结果表

这样做的好处：

- 原始问题排查更直接
- 指标口径更容易统一
- 补数和重算更容易定位到某一层
- 下游系统不需要直接接触原始表
### 6.6 基于 ClickHouse 的典型分析场景

ClickHouse 建议优先支撑以下分析：

- **投放报表分析**：按日期、渠道、账户、Campaign、素材、国家查看消耗、点击、转化、ROI、ROAS
- **漏斗分析**：点击 -> 下载 -> 激活 -> 注册 -> 付费
- **留存分析**：D1、D3、D7、D15、D30 留存
- **LTV 分析**：按渠道、国家、素材、包版本看回收曲线
- **归因分析**：媒体侧点击和业务侧结果关联分析
- **异常分析**：花费突增、转化率突降、素材衰减、回传缺失

### 6.7 ClickHouse 建设注意事项

- 不建议把所有分析都压到一张超级大宽表
- 不建议大量依赖临时复杂 Join 做高频报表
- 需要提前统一时区、币种、账户映射、国家映射、素材映射
- 对补数、修数、去重场景，要提前设计好表引擎和重算方案
- 近 30 到 90 天热数据建议保留高性能查询能力，更老数据可归档或降频查询

### 6.8 表引擎与分区建议

建议按场景选择表引擎：

- `原始事件表`
  - 优先 `MergeTree`
- `标准事实表`
  - 若存在迟到修正、重复覆盖，可考虑 `ReplacingMergeTree`
- `聚合主题表`
  - 口径稳定时可考虑 `SummingMergeTree` 或 `AggregatingMergeTree`

常见建议：

- 分区：`toYYYYMM(event_time)` 或 `toYYYYMM(stat_date)`
- 排序键：按高频过滤维度设计，如 `(app_id, event_name, event_time)` 或 `(stat_date, channel, account_id, campaign_id)`
- 不要照搬 MySQL 主键和索引设计

---

## 七、功能模块拆分建议

### 7.1 第一阶段：分析底座

目标：把 Meta 数据分析底座打稳。

建议优先做：

- Meta 广告数据采集
- 业务转化数据接入
- 账户 / Campaign / 素材 / 国家 / 时间维度打通
- 核心指标计算
- 日报 / 周报 / 看板
- 数据异常监控

### 7.2 第二阶段：投放分析平台

目标：把人工分析流程产品化。

建议建设：

- 多维筛选报表
- ROI / ROAS 看板
- 素材分析面板
- 账户诊断面板
- 国家与时段表现分析
- 客户级别数据隔离

### 7.3 第三阶段：自动化分析

目标：把分析结果进一步用于业务动作。

建议建设：

- 花费异常监控
- 转化异常监控
- 素材衰减识别
- 自动诊断结论生成
- 预算调整建议
- 放量与止损建议

这一阶段建议明确与下游系统的关系：

- 给 `DMP`
  - 输出用户状态事实、行为聚合和价值分层输入
- 给 `营销中台`
  - 输出用户阶段状态、转化回流、可触达判断辅助结果
- 给 `自动化策略平台`
  - 输出 ROI、ROAS、CVR、异常信号和预算建议输入

### 7.4 第四阶段：算法与智能优化

目标：形成长期护城河。

建议建设：

- 预算自动优化
- 素材评分与推荐
- 转化率预测
- LTV 预测
- Campaign 结构优化

---

## 八、接口与任务体系建议

### 8.1 定时任务

| 任务 | 频率 | 说明 |
|-----|------|------|
| 广告平台明细拉取 | 每小时 / 每日 | 拉取广告消耗、点击、转化明细并写入 `raw` |
| 业务结果同步 | 每小时 / 每日 | 拉取注册、付费、留存等业务结果并写入 `raw` |
| 原始事件标准化 | 实时 / 准实时 | 从 `raw` 进入 `dwd`，统一字段、时区、币种和事件口径 |
| 维度映射刷新 | 每日 | 账户、Campaign、素材、国家映射更新 |
| 主题汇总构建 | 每小时 / 每日 | 生成 `dws` 小时表、日报表、漏斗表、回收表 |
| ADS 宽表构建 | 每小时 / 每日 | 生成 BI、DMP、营销中台、自动化平台直接消费结果 |
| 异常检测任务 | 每小时 | 识别花费、转化、素材异常 |
| 日报生成 | 每日 | 输出客户与内部运营日报 |

### 8.2 对内 API

建议逐步建设以下接口：

- 指标查询接口
- 维度筛选接口
- 报表导出接口
- 异常事件查询接口
- 自动诊断接口
- 预算建议接口

### 8.3 补数与重算机制

当前阶段建议把补数与重算当成标准能力，而不是临时脚本。

建议至少支持：

- 按 `日期` 重算
- 按 `app_id / client_id` 重算
- 按 `渠道 / 账户 / Campaign` 重算
- 按 `某一层级` 重算，如只重算 `dws` 或 `ads`

推荐原则：

- 原始数据尽量只追加，不直接手工改
- 修数通过重跑标准 SQL 完成
- 重算任务要有日志、版本和影响范围记录

---

## 九、部署建议

### 9.1 启动期部署

| 组件 | 建议 |
|-----|------|
| Python 任务服务 | 1 台应用机即可起步，负责外部接入和调度脚本 |
| PostgreSQL | 托管数据库优先，负责配置、权限、任务和元数据 |
| ClickHouse | 作为事件主底座和分析主库，单节点起步，后续按量扩展 |
| 对象存储 | S3 或兼容对象存储 |
| 调度器 | Airflow / Dagster，早期也可先 cron |
| BI 工具 | Metabase |
| API 服务 | FastAPI |

### 9.2 启动期数据刷新建议

- 核心明细：小时级
- 核心日报：天级
- 异常检测：小时级
- 管理看板：日级即可

当前阶段不必过度追求分钟级实时，优先保证：

- 数据完整
- 指标统一
- 查询稳定
- 报表可信

### 9.3 与下游平台的关系

分析平台当前不只是报表系统，而是多个上层系统的公共数据服务层：

- `DMP`
  - 消费用户状态事实、行为聚合、价值标签输入
- `营销中台`
  - 消费用户阶段状态、渠道转化结果、回流复盘数据
- `自动化策略平台`
  - 消费 ROI、ROAS、CVR、异常信号、预算建议输入
- `BI / 管理看板`
  - 消费日报、小时报、账户宽表、客户宽表

---

## 十、安全与权限建议

### 10.1 权限原则

- 按客户隔离数据
- 按角色控制可见范围
- 核心指标口径只允许中心化维护
- 原始明细访问权限默认收敛

### 10.2 审计与备份

- 保留任务执行日志
- 保留原始数据归档
- 关键汇总表支持重算
- 保留指标版本记录

---

## 十一、实施顺序建议

### 11.1 0 到 1 推荐路径

```text
第 1 步：统一 raw 层接入广告、业务和事件采集系统数据
第 2 步：在 ClickHouse 内建立 dwd 标准事实表
第 3 步：统一账户 / 国家 / 素材 / 时间 / 币种维度
第 4 步：产出 dws 主题汇总表和 ads 应用宽表
第 5 步：出 Metabase 看板、日报和小时报
第 6 步：做异常监控、自动诊断和补数重算机制
第 7 步：开放 API 给 DMP、营销中台和自动化策略平台
第 8 步：再进入预算优化、素材算法、预测模型
```

### 11.2 不建议的顺序

不建议：

- 先做复杂算法，后补数据底座
- 先做自研 BI 大前端，后补指标体系
- 先做多渠道统一平台，Meta 基础却没打透

---

## 十二、最终建议

对于 AZSL 当前阶段，最合适的不是重型大数据平台，而是一套可快速落地、又能平滑演进到自动化和 AI 的轻中台方案。

推荐结论如下：

- **先把 ClickHouse 建成事件事实主底座和分析主引擎**
- **核心存储采用 ClickHouse + PostgreSQL**
- **数据接入和调度以 Python 为主，主要分析计算尽量在 ClickHouse 内完成**
- **看板和分析先用 Metabase**
- **调度用 Airflow 或 Dagster**
- **接口层用 FastAPI**
- **后续再逐步扩展到多渠道、DMP、营销中台、自动化和算法能力**

一句话概括：

> 先把“ClickHouse 事件底座、指标统一、报表分析、异常监控、补数重算”做扎实，再把这些数据能力变成 DMP、营销中台、自动化投放和 AI 优化的基础设施。
